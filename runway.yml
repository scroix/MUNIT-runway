version: 0.3
python: 3.8
entrypoint: python runway_model.py
cuda: 11
spec:
    cpu: False
    gpu: True
build_steps:
    - pip install runway-python
    - pip install torch==1.7.0 torchvision torchfile
    - pip install tensorboard tensorboardx
    - pip install -r requirements.txt
